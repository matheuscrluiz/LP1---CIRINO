/*9) Escrever um programa em C que calcule e imprima o número de caracteres distintos presentes em uma cadeia de caracteres dada. Considerar o tamanho máximo da cadeia como sendo de 80 colunas.*/

#include <stdio.h>

int main (void)
{
	char string[80];
	int i, count=0;

	printf("\nDigite uma palavra de até 80 letras: ");
	scanf("%s", string);

	for(i=0;string[i] != '\0';i++)/*Rodar no for até que seja ñ seja encontrado uma letra na string*/
		{
			if(string[i] != '\0')
				{
					count++;
				}
		}

	printf("\nA quantidade de letras digitadas na frase ' %s ' é de: %d ", string, count);

	return 0;
}
